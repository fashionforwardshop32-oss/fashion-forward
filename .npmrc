# Install `file:` dependencies (vendor/photon-runtime) as real directories in
# node_modules instead of symlinks. Next.js only treats a package listed in
# `serverExternalPackages` as external when it resolves inside node_modules;
# a symlink resolves to vendor/ and gets bundled by webpack instead.
install-links=true
